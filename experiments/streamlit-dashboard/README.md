# Streamlit dashboard (early alternate UI)

From `archive/frontend-dashboard`. A single-file Streamlit investigator view built
before the React console in `frontend/`. Kept for reference — the React console is
the primary UI.

```bash
pip install -r requirements.txt
streamlit run app.py
```
